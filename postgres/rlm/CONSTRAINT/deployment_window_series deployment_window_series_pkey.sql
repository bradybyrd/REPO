-- Name: deployment_window_series deployment_window_series_pkey; Type: CONSTRAINT; Schema: public; Owner: deploy
--
ALTER TABLE ONLY public.deployment_window_series
    ADD CONSTRAINT deployment_window_series_pkey PRIMARY KEY (id);
--
