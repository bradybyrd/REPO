-- Name: deployment_window_series_id_seq; Type: SEQUENCE OWNED BY; Schema: public; Owner: deploy
--
ALTER SEQUENCE public.deployment_window_series_id_seq OWNED BY public.deployment_window_series.id;
--
